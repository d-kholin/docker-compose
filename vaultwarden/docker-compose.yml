services:
  vaultwarden:
    image: vaultwarden/server:1.33.2
    restart: always
    volumes:
      - "bw:/data"
    ports:
      - "8442:80"
    env_file:
      - ../stack.env
volumes:
  bw: {}