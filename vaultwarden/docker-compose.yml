services:
  vaultwarden:
    image: vaultwarden/server:1.36.0
    restart: always
    volumes:
      - "bw:/data"
    ports:
      - "8442:80"
    env_file:
      - ../stack.env
volumes:
  bw: {}