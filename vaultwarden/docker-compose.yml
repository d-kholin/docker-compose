services:
  vaultwarden:
    image: vaultwarden/server:1.37.0
    restart: always
    volumes:
      - "bw:/data"
    ports:
      - "8442:80"
    env_file:
      - ../stack.env
volumes:
  bw: {}