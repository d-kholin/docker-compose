# OpenCloud — vars to add to your shared ../stack.env
# Copy these into stack.env on the host (never commit stack.env to git).

OC_DOMAIN=cloud.example.com
COLLABORA_DOMAIN=office.example.com

OC_ADMIN_PASSWORD=changeme

COLLABORA_ADMIN_USER=admin
COLLABORA_ADMIN_PASSWORD=changeme